∀(k) | 0<=k<=150 {
    norm = Σ(A[i][k] * A[i][k]) | 0<=i<=250
    R[k][k] =  √(norm)
    ∀(i) | 0<=k<=150 {
        Q[i][k] = A[j][k] / R[k][k]
    }
    ∀(j) | k+1<=j<=150 {
        R[k][j] = Σ(Q[i][k] * A[i][j]) | 0<=i<=250
        A[i][j] = A[i][j] - Q[i][k] * R[k][j]
    }
}
